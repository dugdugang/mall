<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dg.mall.mapper.ProductCustomMapper">
    <select id="getProductCustoms" resultType="">
SELECT a.id,a.sn,a.name,b.name as categroy_name ,a.market_price,a.price,a.score_count,a.is_top,a.create_date FROM product as a LEFT JOIN product_category as b ON a.product_category=b.id
LIMIT 0,20;
</select>
</mapper>